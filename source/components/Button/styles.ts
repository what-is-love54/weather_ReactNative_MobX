/** @format */

import { StyleSheet } from 'react-native';
// common
import { colors } from '~/constants';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: colors._F56B63,
    height: 60,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
    borderRadius: 15,
  },
});
