/** @format */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  permissionText: {
    marginTop: 50,
  },
  permissionButton: {
    marginTop: 50,
  },
  settingsButton: {
    marginTop: 20,
  },
});
